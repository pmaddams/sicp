The procedure

(define (f g) (g 2))

can be rewritten as:

(define f (lambda (g) (g 2)))

Therefore,

(f f)

evaluates to:

(lambda (f) (f 2))
(lambda (f) (lambda (2) (2 2)))

resulting in an error, since 2 is neither a valid identifier nor a procedure.
